In the disposable repository and branch named in the parent instruction, create or replace `kanedias-e2e-marker.txt` with the supplied unique marker, commit it, push the exact branch to `origin`, and invoke `handoff` alone with the repository slug, pre-change base commit, exact branch, exact pushed HEAD, summary, and verification. This environment is explicitly disposable; do not touch any other repository or branch.
