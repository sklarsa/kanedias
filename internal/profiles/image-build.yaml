config:
  security.nesting: "true"
  security.privileged: "false"
description: Unprivileged container with nesting for Docker and kind
devices:
  eth0:
    name: eth0
    network: kanedias
    type: nic
